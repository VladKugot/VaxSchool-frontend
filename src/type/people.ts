export type Student = {
    id: string;
    firstName: string;
    lastName: string;
    birthDate: string;
    studentClass: string;
    pageNumber: string;
}

export type Teacher = {
    id: number;
    firstName: string;
    lastName: string;
    position: string;
    birthDate: string;
    pageNumber: string;
};